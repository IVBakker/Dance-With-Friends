doctype html
html
	head
		title Dance with Friends
		// Bootstrap
		link(href='/static/css/bootstrap.min.css', rel='stylesheet', media='screen')
		link(rel='stylesheet/less', type='text/css', href='/static/less/styles.less')
		script(src='/static/js/lib/less.min.js', type='text/javascript')
		script(type='text/javascript', src='/static/js/lib/jquery.min.js')
		script(src='/static/js/lib/jquery-ui.min.js', type='text/javascript')
	body#backk
		.row-fluid.fill-vert
			.span4
			.span4.fill-vert.middle
				#logo
				.row-fluid
					a.gen-button.btn.span12(href='/dance') I CAME HERE TO DANCE
		script(type='text/javascript').
			//setup title
			$('#logo').height($('#logo').width() / 2.033333);
			$(document).ready(function() {
				var socket = io(location.protocol+'//'+location.hostname+(location.port ? ':'+location.port: ''));
				socket.on('news', function (data) {
					console.log(data);
					socket.emit('my other event', { my: 'data' });
				});
			});